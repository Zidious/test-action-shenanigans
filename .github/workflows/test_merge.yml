name: test merge

on:
  pull_request:
    branches: master

jobs:
  if_merged:
    if: github.event.pull_request.merged == 'true'
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
